from distutils.core import setup

setup(
    name='pyratebeer',
    version='0.1dev',
    packages=['pyratebeer',],
    license=open('LICENSE').read(),
    long_description=open('README.md').read(),
)
